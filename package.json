{
    "name": "chip8-ts",
    "version": "0.1.0",
    "scripts": {
        "compile": "tsc -p .",
        "watch": "tsc --watch -p .",
        "test": "tsc -p ./test && mocha --recursive ./test/**/*.js",
        "tslint": "tslint -c tslint.json 'ts/**/*.ts' 'test/**/*.ts'"
    },
    "dependencies": {
        "ts-loader": "^0.8.2"
    },
    "devDependencies": {
        "mocha": "3.1.0",
        "expect.js": "0.3.1",
        "@types/node": "^6.0.41",
        "@types/mocha": "2.2.32",
        "@types/expect.js": "0.3.29"
    }
}